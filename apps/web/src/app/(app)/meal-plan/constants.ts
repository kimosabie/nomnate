export const FREE_AI_LIMIT = 7;

// How many main options per day the "Plan my whole week with AI" action fills
// (cost control — fewer dishes per tap). Max 14 mains across the week.
export const AI_WEEK_OPTIONS_PER_DAY = 2;
